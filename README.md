comparing a regular forward and backward pass kernel that retrieves all weights from memory with a kernel that generates weights in the kernel for a majority of the weight matrix and has a small subset of trainable
parameters. generating weights is faster. 
